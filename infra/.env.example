# Copier ce fichier en .env (non versionné) et ajuster selon vos ports locaux.

# Connexion Postgres (Compose par défaut : user/mot/pass du docker-compose.yml)
DATABASE_URL=postgresql://gestion:gestion@localhost:5432/gestion_mvp

# Plus tard Nest + Next :
# JWT_SECRET=
# NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# MOLLIE_API_KEY=
# S3_BUCKET=
# S3_ENDPOINT=
